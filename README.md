# dbcli

`dbcli` is a recipe-first CLI for loading CSV/XLSX files into MySQL tables.

The v1 implementation contract lives in [`SPEC.md`](SPEC.md). The build sequence lives in [`BUILD.md`](BUILD.md).
